{
  "name": "todo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "frontend": "cd frontend && yarn start",
    "server": "cd server && nodemon src/index.js",
    "dev": "set NODE_ENV=dev && concurrently --kill-others-on-fail \"yarn server\" \"yarn frontend\"",
    "start": "set NODE_ENV=production && cd server && node index.js",
    "build": "cd frontend && yarn install && yarn install --only=dev --no-shrinkwrap && yarn build"
  },
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
